// Phase 5.1 — window scaffold only. Phase 5.2 will render the coordinate
// box + bubble; Phase 5.3 wires the listen() side that brings the data in.
export default function Overlay() {
  return (
    <div className="overlay-root">
      <p className="overlay-placeholder">overlay (Phase 5.2 will render here)</p>
    </div>
  );
}
