# ScreenBridge — Product Blueprint

## One-Liner
AI가 시키는 추상적 지시를 사용자의 실제 화면에 맞는 구체적 지시로 실시간 번역하는 macOS 데스크톱 도구.

## 핵심 문제 (변경 금지)
AI 도구를 매일 쓰는 사람들의 공통 마찰:
- AI가 절차를 알려준다 ("X 메뉴에서 Y 클릭")
- 사용자의 실제 화면이 그것과 안 맞는다 (버전 차이, 플랜 차이, UI 변경, 본인 계정 상태)
- 사용자는 "뭐가 어디 있는지" 매번 직접 매핑
- AI 대화 컨텍스트가 다음 AI 세션으로 안 넘어감
- 결과: AI를 쓰면서도 사용자가 모든 실행 부담을 짊어진다

이 격차 = "실행 번역 (Execution Translation)" 문제.

## 타겟 사용자
나 (제작자). 매일 여러 AI 도구로 일하면서 AI 지시 ↔ 실제 화면 매핑에 시간 낭비하는 사람. 다른 사용자는 고려하지 않음. 내가 매일 쓸 정도로 좋아지면, 그때 다른 사람을 생각한다.

## 무엇이 아닌가
- ❌ Cluely 같은 "AI한테 답 받기" 도구 (답 모르는 사람용)
- ❌ 완전 자율 에이전트 (사용자 통제 우회)
- ❌ 일반 OCR/스크린샷 도구
- ✅ "AI 지시 ↔ 내 실제 화면" 사이 번역 레이어

## 기술 스택 (확정)
- 앱 셸: Tauri 2.0 (Rust 백엔드 + WebView 프론트)
- 프론트엔드: React + TypeScript
- LLM (초기): Anthropic API, claude-sonnet-4-6 vision
- LLM 추상화: dispatcher 인터페이스 — 미래에 로컬 LLM 교체 가능
- 데이터 저장: 전부 로컬, 외부 전송 0

## 핵심 기술 철학: LLM은 commodity, Context Pipeline이 moat

본 제품의 가치는 LLM 자체에 있지 않다. LLM은 교체 가능한 컴포넌트.

진짜 IP:
1. 컨텍스트 소스 통합 — 화면, AI 대화, 앱 상태, (장기) 오디오
2. 시간 동기화/필터링 — 무엇을 언제 LLM에 보낼지
3. 출력 디스패치 — 결과를 어떤 형태로 사용자에게

설계 원칙:
- 모든 컨텍스트 소스 = 독립 모듈
- LLM 호출 레이어 = 단일 인터페이스, 모델 스왑 가능
- 프롬프트 = 별도 파일, 버전 관리
- 새 소스 추가 = 새 모듈 추가, 다른 코드 변경 최소

## 제품 단계별 청사진

### v0.1 — 번역기 (지금 작업 중)
- 트리거: 글로벌 단축키 (기본 ⌥+Space)
- 입력: 현재 화면 캡처 + 사용자가 텍스트로 붙여넣는 AI 지시
- 출력: 화면 위 반투명 오버레이 (텍스트 가이드 + 좌표 박스)
- 단일 macOS Space만 지원

진입 조건 → v0.2: 내가 일주일에 5번 이상 자발적으로 씀.

### v0.2 — 자동 컨텍스트 수집
- 클립보드 모니터링 (AI 응답 복사 시 자동 캐치)
- 활성 앱 인식
- 단축키 1회로 캡처+분석+오버레이

### v0.3 — 브라우저 통합
- Chrome/Safari 확장: ChatGPT/Claude.ai/Gemini 응답 DOM에서 자동 추출

### v0.4 — 오버레이 폴리시
- CSS 애니메이션, 그라데이션, 다중 요소 강조

### v0.5 — Spaces 멀티 데스크톱 세션
- 여러 macOS Space를 하나의 "작업 세션"으로 묶음

### v0.6+ — Audio Context (조건부 진입)
진입 조건: v0.1-0.5 매일 사용, "회의/통화에도 있으면" 페인이 주 3회 이상.

### v0.7+ — LLM Sovereignty
진입 조건: 6개월+ 사용, 데이터셋 1000+ 튜플 누적, 비용/프라이버시 진짜 우려.
- 로컬 vision 모델 fine-tuning
- Ollama / llama.cpp 통합
- Hybrid → 완전 로컬 점진 이전

## 비-기능 원칙
1. 개인정보 우선: 스크린샷 매우 민감. 모든 데이터 로컬. 사용자 wipe 가능.
2. 인간 통제 유지: 자동 실행은 옵션. 기본 = 인간 실행.
3. 사용자 = 본인 (dogfooding): 매일 안 쓰면 출시 안 함.

## 성공 신호
- v0.1: 일주일 5회 이상 자발적 사용
- v0.2: 다른 AI 도구 화면 캡처+붙여넣기 횟수 50% 감소
- v0.3+: 사용 중 "이 기능 있으면 좋겠다" 자연 발생

지표 = 본인 행동만.
